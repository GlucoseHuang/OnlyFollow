name: OnlyFollow
options:
  bundleIdPrefix: com.personal
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

settings:
  base:
    SWIFT_VERSION: "5.0"
    DEVELOPMENT_TEAM: "WUWTLNTANV"

packages:
  Starscream:
    url: https://github.com/daltoniam/Starscream
    from: "4.0.0"
  Kingfisher:
    url: https://github.com/onevcat/Kingfisher
    from: "7.0.0"

targets:
  OnlyFollow:
    type: application
    platform: iOS
    sources:
      - OnlyFollow
    settings:
      base:
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: OnlyFollow/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.personal.OnlyFollow
        PRODUCT_NAME: OnlyFollow
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: 1
        SWIFT_EMIT_LOC_STRINGS: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: "WUWTLNTANV"
    dependencies:
      - package: Starscream
      - package: Kingfisher
