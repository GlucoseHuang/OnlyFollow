# 岚岚的视频信息流 App —— 需求文档 v0.1

> 最后更新：2026-06-10  ·  状态：初稿，待与 Codex 同步后迭代

---

## 0. 项目原则（最重要，写在最前）

**这是一个"反推荐"工具，不是另一个视频 App。**

- 它的存在意义，是让我（用户）能**只看自己主动关注的人**，不被任何算法推荐打扰
- 任何功能决策（包括但不限于：内容展示、跳转逻辑、入口设计）都必须回答一个问题：**会不会让用户无意识地进入信息流？**
- 如果一个功能会让我"不小心又刷到了算法推荐的内容"，那就**不做**或者**必须用最显眼的确认动作保护我**

---

## 1. 项目愿景

做一个**完全自用**的 iOS / iPadOS App，聚合我关注的抖音主播和 B 站 UP 主的所有内容（视频 + 直播 + 评论 + 弹幕），**没有推荐算法，没有信息流**，**只服务于我主动关注的人**。

不签名、不上架、不分发。仅通过 Xcode 在自己的 iPhone 和 iPad 上安装测试版。

---

## 2. 目标设备

- iOS 16+ / iPadOS 16+
- iPhone（通勤、碎片时间）
- iPad（居家、长时间观看直播）

---

## 3. 支持的内容源

| 平台 | 状态 | 备注 |
|------|------|------|
| 抖音 | ✅ 支持 | 自用场景，接口抓取无合规压力 |
| 哔哩哔哩 | ✅ 支持 | B 站有相对开放的接口，社区资源丰富 |

后续可扩展其他平台，但 v0.1 不考虑。

---

## 4. 核心场景

| 场景 | 描述 |
|------|------|
| S1：通勤刷更新 | iPhone 上打开 App，看到关注的 UP 主最近发了什么新视频 |
| S2：睡前看直播 | iPad 躺床上看关注的游戏区主播直播，跟着实时弹幕 |
| S3：补历史视频 | 点开某个 UP 主主页，看 ta 之前的视频列表，挑感兴趣的一个看 |
| S4：找新主播 | 通过搜索发现新的主播/UP 主，输入 UID 添加关注 |
| S5：管理关注 | 查看、移除已关注的人 |

---

## 5. 功能需求

### 5.1 关注管理

- ✅ **添加关注**：输入抖音/B 站 UID 或主页链接，识别并关注
- ✅ **查看关注列表**：显示所有已关注的人，含头像、昵称、平台
- ✅ **移除关注**：长按或左滑删除
- ❌ 不做分组（v0.1 暂不支持）
- 关注数据存储在**本地**（不上云），备份策略待定

### 5.2 内容浏览

#### 5.2.1 内容流

- App 启动后，默认进入**"我的关注"**页
- 按平台 tab 分开：抖音 | B 站
- 关注列表中，区分两种状态：
  - **正在直播**：显示直播间入口 + 实时观看人数
  - **有新视频**：显示最新 N 个视频缩略图 + 标题
- 排序：直播置顶，其余按更新时间倒序

#### 5.2.2 视频观看

- **优先在 App 内播放**（如不可行，见 §7 风险与备选方案）
- 视频页支持：
  - 播放/暂停/进度条
  - 横竖屏切换
  - **历史弹幕**（以滚动条形式叠加在视频上）
  - **历史评论**（视频下方的列表页/抽屉）
- ❌ 不需要缓存视频
- ❌ 不需要发送弹幕或评论（v0.1 暂不支持）

#### 5.2.3 直播观看

- **优先在 App 内播放**（如不可行，见 §7 风险与备选方案）
- 直播页支持：
  - 实时视频流
  - **实时弹幕**（必须，长连接）
  - 直播间信息（标题、主播、观看人数）
  - 横屏沉浸模式
- ❌ 不需要发送弹幕（v0.1 暂不支持）

### 5.3 通知

- ❌ v0.1 暂不实现推送通知
- 后续可加：关注的 UP 主开播/更新时本地通知

### 5.4 搜索

- ✅ 支持输入 UID 搜索抖音用户
- ✅ 支持输入 UID 搜索 B 站用户
- ❌ 不做内容搜索（不搜视频/弹幕）

---

## 6. 非功能需求

| 类别 | 要求 |
|------|------|
| 隐私 | 不收集任何用户数据；不接入任何统计 SDK；不连任何第三方服务（除抖音/B 站接口） |
| 账号 | 不需要登录抖音/B 站账号（v0.1） |
| 启动速度 | 冷启动 < 2s |
| 离线 | App 在无网时显示空状态，不崩溃 |
| 数据存储 | 关注列表本地保存；视频元数据/评论/弹幕按需拉取，不做缓存（v0.1） |

---

## 7. 风险与备选方案

### 7.1 抖音视频/直播源

- 抖音没有官方开放 API，视频源需要通过逆向接口或网页抓取
- **风险**：接口签名算法可能随时变更，维护成本不低
- **备选**：如 App 内播放直播/视频技术难度过大，**跳转到抖音 App 对应页面**——但跳转时需用 URL Scheme 显式跳转到指定视频/直播间，避免落入信息流

### 7.2 抖音直播流格式

- 抖音直播流（FLV/HLS）的获取和解码在 iOS 上有成熟方案，但需要持续跟进签名变化

### 7.3 B 站直播弹幕协议

- B 站直播弹幕使用 WebSocket + 自有协议，社区有成熟开源实现参考，**风险较低**

### 7.4 视频清晰度

- 抖音/B 站可能对非官方客户端限制清晰度（限制到 720p 或更低）
- **接受此限制**（自用，不追求画质极限）

---

## 8. 明确不做的事

为了保护项目不偏离"反推荐"原则，以下功能**明确不做**：

- ❌ 推荐算法 / 热门 / 猜你喜欢
- ❌ 关注的人的"好友也在关注"
- ❌ 评论/弹幕的发布功能（v0.1）
- ❌ 视频下载/缓存（v0.1）
- ❌ 推送通知（v0.1）
- ❌ 关注分组（v0.1）
- ❌ 登录态（v0.1）
- ❌ 内容搜索（v0.1）
- ❌ 暗色模式以外的任何视觉风格探索（先用系统默认）

---

## 9. 后续可探索（v0.1 不做，但记录在这里）

- iCloud 同步关注列表
- 多设备实时同步
- 关注的 UP 主开播/更新时本地通知
- 视频下载离线看
- 弹幕/评论的发送
- 关注分组
- Widget（桌面小组件，显示正在直播的人）
- Apple Watch 极简通知

---

## 10. 待 Codex 评估的技术问题

把这些问题打包给 Codex 去做技术摸底，输出实现方案：

1. 抖音用户信息/视频列表/评论/直播流的接口可用性
2. 抖音直播流在 iOS 上的播放方案（AVPlayer 是否够用）
3. B 站 WebSocket 弹幕协议的接入方式
4. SwiftUI vs UIKit 的技术选型建议
5. 项目脚手架（Xcode 工程结构、依赖管理）
6. 数据持久化方案（SwiftData vs Core Data vs UserDefaults）
7. UI/UX 草图（MVP 的页面结构）

---

## 附录 A：术语表

| 术语 | 含义 |
|------|------|
| UID | 用户 ID，平台内唯一标识一个人 |
| UP 主 | B 站对内容创作者的称呼 |
| 弹幕 | 视频/直播上飘过的实时或历史评论 |
| 信息流 | 算法推荐的连续内容列表（本 App 明确反对） |
| 自用 | 仅作者本人使用，不签名、不上架、不分发 |
