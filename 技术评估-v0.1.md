# OnlyFollow 技术评估 v0.1

> 评估日期：2026-06-10 · 对应需求文档 v0.1

---

## 一、总体可行性判断

**项目可行，但抖音侧的维护成本是核心风险。**

B 站侧接口和协议成熟、社区资源丰富，实现难度低。抖音侧没有官方开放 API，依赖网页端逆向接口和签名破解，签名算法随时可能变更。好消息是：社区已有成熟的开源实现可供参考（见下文），且需求文档已明确"自用、不签名、不上架"，这大幅降低了合规压力和风控触发概率。

**一个重要的参考项目：[Simple Live](https://github.com/xiaoyaocz/dart_simple_live)**——基于 Dart/Flutter 的跨平台直播聚合客户端，已支持抖音 + B 站直播 + 弹幕 + 关注管理，功能与本项目高度重叠。其 `simple_live_core` 层的实现逻辑可直接参考移植到 Swift。

---

## 二、7 个技术问题的逐项评估

### 2.1 抖音用户信息 / 视频列表 / 评论 / 直播流的接口可用性

#### 用户信息 & 视频列表

抖音网页端暴露了以下 REST 接口（来自 [sshiqiao/douyin-objectc](https://sshiqiao.github.io/document/douyin-objectc.html) 及 [Evil0ctal/Douyin_TikTok_Download_API](https://github.com/Evil0ctal/Douyin_TikTok_Download_API)）：

| 功能 | 接口路径 | 返回内容 | 签名要求 |
|------|----------|----------|----------|
| 用户详情 | `/aweme/v1/web/user/profile/` | 昵称、头像、粉丝数、作品数 | X-Bogus / A-Bogus + msToken |
| 用户视频列表 | `/aweme/v1/web/aweme/post/` | aweme_id、描述、封面、播放地址、统计 | 同上 |
| 单个视频详情 | `/aweme/v1/web/aweme/detail/` | 视频播放地址、评论数、分享数 | 同上 |
| 视频评论 | `/aweme/v1/web/comment/list/` | 评论内容、用户、时间、点赞 | 同上 |

**签名机制**：抖音网页端请求需要附加 `X-Bogus` 或 `A-Bogus` 参数 + `msToken` Cookie。这些签名由 JS 脚本在浏览器端生成，算法会不定期更新。

**获取签名的方式**：
- **方案 A（推荐）**：内嵌一个隐藏的 WKWebView，加载抖音网页端 JS，通过 `evaluateJavaScript` 调用签名函数生成参数。MediaCrawler 项目用的就是类似思路（Playwright + JS 表达式获取签名）。优点是无需硬编码签名算法，JS 更新后自动生效。
- **方案 B**：纯 JS 逆向，将签名算法移植到 Swift。性能好但维护成本高，算法一变就要重做。
- **方案 C**：使用 TikHub.io 等第三方付费 API。稳定但依赖第三方且收费。

**自用场景下方案 A 最优**——维护负担最低，抖音更新 JS 后 App 自动适配。

#### 直播流

| 步骤 | 接口 / 方法 | 说明 |
|------|-------------|------|
| 判断是否开播 | `/webcast/room/web/enter/` | 传入 room_id，返回直播状态、房间信息 |
| 获取拉流地址 | 同上接口，或 `/webcast/room/web/get_info/` | 返回 FLV / HLS 拉流 URL |
| 直播间信息 | 返回数据中包含 title、user_count、cover 等 | |

抖音直播流通常同时提供 FLV（低延迟 ~3s）和 HLS（高延迟 ~10s）两种格式。

**风险等级：🟡 中**——接口可用，但签名维护是持续负担。方案 A 可大幅降低此风险。

---

### 2.2 抖音直播流在 iOS 上的播放方案

| 播放器 | FLV | HLS | RTMP | 延迟 | 维护状态 | 说明 |
|--------|-----|-----|------|------|----------|------|
| **AVPlayer**（系统） | ❌ | ✅ | ❌ | ~10s | Apple 维护 | 只能播 HLS，延迟高，不适合互动直播 |
| **IJKPlayer**（bilibili/ijkplayer） | ✅ | ✅ | ✅ | ~3s | ⚠️ 已停更 | 基于 FFmpeg，B 站出品，有 CocoaPods 和 Swift 示例 |
| **MPV / 自编译 FFmpeg** | ✅ | ✅ | ✅ | ~3s | 社区维护 | 灵活但编译复杂 |
| **跳转抖音 App** | N/A | N/A | N/A | N/A | — | 备选方案，用 URL Scheme 跳转 |

**推荐方案：IJKPlayer**

理由：
- B 站出品，专为中文直播场景优化，FLV 播放成熟
- 有现成的 CocoaPods 集成方案和 Swift Demo
- 虽然主仓库已停更，但社区 fork 持续维护，且直播协议（FLV/HLS）本身稳定，不太需要频繁更新
- 对于点播视频（抖音视频、B 站视频），也可用 IJKPlayer 统一播放

**备选**：如果 IJKPlayer 集成遇阻，可在直播场景退回 AVPlayer 播 HLS（接受 ~10s 延迟），或使用腾讯云 TXLivePlayer SDK（功能强大但体积大）。

**点播视频**：抖音和 B 站的点播视频通常提供 MP4 直链，AVPlayer 即可播放。但为统一体验，建议全用 IJKPlayer。

---

### 2.3 B 站 WebSocket 弹幕协议的接入方式

B 站弹幕协议文档极为完善，社区实现丰富。

#### 协议流程

```
1. HTTP GET /xlive/web-room/v1/index/getDanmuInfo → 获取 token、host
2. WebSocket 连接 wss://host/sub → 建立长连接
3. 发送 Auth 包（op=7，含 roomid + key + uid 等）
4. 每 30s 发送心跳包（op=2，5字节）
5. 接收弹幕包（op=5），解压 + 解码
```

#### 数据帧格式（二进制）

```
Offset  Size  Field
0       4     数据包长度（大端）
4       2     Header 长度（固定 16）
6       2     ProtoVer（0=JSON, 2=Zlib压缩JSON, 3=ProtoBuf）
8       4     Operation（3=AuthReply, 5=Command, 8=HeartbeatReply）
12      4     Sequence ID
16+     ...   Payload
```

- ProtoVer=2 时 payload 为 Zlib 压缩的 JSON
- ProtoVer=3 时 payload 为 ProtoBuf 编码（需 `.proto` 文件）
- 当前生产环境主要使用 ProtoVer=2（JSON），实现更简单

#### Swift 实现路径

| 组件 | 选择 | 说明 |
|------|------|------|
| WebSocket | **Starscream** | 最成熟的 Swift WebSocket 库，SPM 支持 |
| 二进制解析 | `Data` + 手动读取 | 帧头固定 16 字节，手动解析即可 |
| Zlib 解压 | `Compression`（系统框架） | iOS 原生 zlib 支持 |
| ProtoBuf（如需） | **SwiftProtobuf** | Google 官方 Swift ProtoBuf 库 |

#### 参考实现

- [lovelyyoshino/Bilibili-Live-API](https://github.com/lovelyyoshino/Bilibili-Live-API) — 协议文档
- [Minteea/bilibili-live-danmaku](https://github.com/Minteea/bilibili-live-danmaku) — TypeScript 实现，含 proto 文件
- [bilibili-API-collect](https://github.com/SocialSisterYi/bilibili-API-collect) — B 站全量 API 文档

**风险等级：🟢 低**——协议稳定、文档完善、社区活跃。

---

### 2.4 抖音弹幕协议

抖音直播弹幕使用 **WebSocket + Protobuf** 协议：

```
1. 计算签名 → 获得 wss 连接 URL 和参数（room_id, uniqueId, signature 等）
2. 连接 wss://webcast3-ws-web-lf.bytedance.com/webcast/im/push/v2/
3. 接收二进制帧 → 解码为 PushFrame
4. PushFrame.payload → gzip 解压 → 解码为 Response
5. Response.messages → 各类型消息体（ChatMessage, MemberMessage, LikeMessage 等）
```

#### Swift 实现路径

| 组件 | 选择 | 说明 |
|------|------|------|
| WebSocket | Starscream | 同 B 站 |
| 签名计算 | WKWebView + JS | 签名函数由抖音 JS 生成，在 WebView 中调用 |
| Protobuf 解码 | **SwiftProtobuf** | 需从抖音 App 逆向获取 .proto 文件 |
| gzip 解压 | `Compression` | 系统原生 |

#### 参考实现

- [skmcj/dycast](https://github.com/skmcj/dycast) — 抖音弹幕姬，TypeScript 实现，含完整 proto 文件和签名逻辑
- [YunzhiYike/douyin-live](https://github.com/YunzhiYike/douyin-live) — Simple Live 项目中引用的抖音直播实现

**风险等级：🟡 中**——协议本身已知，但签名算法可能变更。用 WKWebView + JS 方式调用签名函数可将此风险降到最低。

---

### 2.5 SwiftUI vs UIKit 技术选型

**推荐：SwiftUI 为主 + UIKit 混合（Hybrid 方案）**

| 层面 | SwiftUI | UIKit（UIViewRepresentable 包裹） |
|------|---------|-----------------------------------|
| 列表/导航/设置 | ✅ 主力 | — |
| 关注列表、搜索页 | ✅ | — |
| **视频播放画面** | — | ✅ IJKPlayer 的 UIView 需 UIKit 包裹 |
| **弹幕渲染层** | — | ✅ 弹幕 Canvas 需高性能自定义绘制 |
| 直播间页面（整体布局） | ✅ 外层 | ✅ 视频区域嵌入 UIKit |

**理由**：
- 2025 基准测试显示 SwiftUI 开发速度快 33%，代码量少
- 视频播放器（IJKPlayer）本身是 UIKit 组件，用 `UIViewRepresentable` 包裹即可
- 弹幕渲染需要逐帧绘制，CAShapeLayer / CADisplayLink 是 UIKit 的强项
- iOS 16+ 已满足 SwiftUI 绝大多数 API 需求
- iPad 和 iPhone 的布局差异可用 SwiftUI 的 `horizontalSizeClass` 自然大屏适配

**不建议纯 UIKit**——开发效率低，且本项目 UI 层并不算特别复杂。

---

### 2.6 数据持久化方案

| 方案 | 最低 iOS 版本 | 适合场景 | 本项目评价 |
|------|---------------|----------|------------|
| **UserDefaults** | iOS 2 | 极简 KV | 关注列表 < 50 人时够用，但无结构化查询 |
| **SwiftData** | iOS 17 | 新项目首选 | **需求要求 iOS 16+，不兼容** |
| **Core Data** | iOS 3 | 复杂关系型数据 | 成熟稳定，iOS 16+ 兼容 |
| **SQLite（GRDB）** | iOS 11 | 需要直接 SQL | 过重 |

**推荐方案：视目标 iOS 版本决定**

- **如果愿意将最低版本提升到 iOS 17**：用 **SwiftData**。数据模型极简（只有 FollowedCreator 一个实体），SwiftData 完全胜任，且与 SwiftUI 集成最佳。
- **如果坚持 iOS 16+**：用 **Core Data**。数据模型简单，Core Data 的复杂度完全可控。

数据模型（仅一个实体）：

```
FollowedCreator
  - uid: String（主键）
  - platform: String（"douyin" | "bilibili"）
  - nickname: String
  - avatarURL: String
  - addedAt: Date
```

**建议将最低版本提升到 iOS 17**，使用 SwiftData，简化开发。2026 年 iOS 16 设备占比已很低。

---

### 2.7 项目脚手架与依赖管理

#### Xcode 工程结构

```
OnlyFollow/
├── App/
│   ├── OnlyFollowApp.swift          # @main 入口
│   └── ContentView.swift            # 根视图 / TabView
├── Models/
│   ├── FollowedCreator.swift        # 关注模型（SwiftData）
│   ├── VideoItem.swift              # 视频元数据
│   ├── LiveRoom.swift               # 直播间信息
│   └── DanmakuMessage.swift         # 弹幕消息
├── Services/
│   ├── Douyin/
│   │   ├── DouyinAPIService.swift   # 网页端 API 封装
│   │   ├── DouyinSigner.swift       # WKWebView 签名
│   │   └── DouyinDanmakuService.swift # 抖音弹幕 WebSocket
│   ├── Bilibili/
│   │   ├── BilibiliAPIService.swift # B 站 API 封装
│   │   └── BilibiliDanmakuService.swift # B 站弹幕 WebSocket
│   └── Common/
│       ├── DanmakuWebSocket.swift    # WebSocket 基类
│       └── BinaryFrameParser.swift  # 二进制帧解析
├── Views/
│   ├── FollowListView.swift         # 关注列表（SwiftUI）
│   ├── VideoPlayerView.swift        # 视频播放（UIKit 包裹）
│   ├── LiveRoomView.swift           # 直播间（SwiftUI + UIKit 混合）
│   ├── DanmakuOverlayView.swift     # 弹幕叠加层（UIKit）
│   ├── CreatorDetailView.swift      # UP 主主页
│   └── AddFollowView.swift          # 添加关注
├── Components/
│   ├── DanmakuCanvas.swift          # 弹幕渲染 Canvas
│   ├── VideoPlayerWrapper.swift     # IJKPlayer UIViewRepresentable
│   └── PlatformTabView.swift        # 平台 Tab 切换
└── Resources/
    ├── Protos/                      # .proto 文件
    └── JS/                          # 签名用 JS 脚本（备选）
```

#### 依赖管理：SPM（Swift Package Manager）

| 依赖 | 用途 | 版本建议 |
|------|------|----------|
| **IJKMediaFramework** | FLV/HLS/RTMP 播放 | k0.8.8（CocoaPods，需桥接或手动集成） |
| **Starscream** | WebSocket 客户端 | 4.x |
| **SwiftProtobuf** | ProtoBuf 编解码 | 1.x |
| **Kingfisher** | 图片加载与缓存 | 7.x |
| **Alamofire** | HTTP 网络请求（可选） | 5.x |

> ⚠️ IJKPlayer 没有原生 SPM 支持，需通过 CocoaPods 集成或手动编译 framework。建议项目同时使用 SPM + CocoaPods（不少 iOS 项目都这么做），或使用 XCFramework 手动集成到 SPM。

---

## 三、UI/UX 页面结构（MVP）

```
┌─────────────────────────────────────────┐
│  TabView                                │
│  ┌───────────────┬───────────────┐      │
│  │  抖音 Tab     │  B 站 Tab      │      │
│  ├───────────────┴───────────────┤      │
│  │                                │      │
│  │  🔴 直播中（置顶）              │      │
│  │  ┌──────────────────────────┐ │      │
│  │  │ [头像] 主播名  👁 1.2万   │ │      │
│  │  │ ▶ 进入直播间             │ │      │
│  │  └──────────────────────────┘ │      │
│  │                                │      │
│  │  📹 有新视频                   │      │
│  │  ┌──────────────────────────┐ │      │
│  │  │ [头像] UP主名 · 3小时前   │ │      │
│  │  │ [缩略图1] [缩略图2] [+]  │ │      │
│  │  └──────────────────────────┘ │      │
│  │  ...                           │      │
│  ├────────────────────────────────┤      │
│  │  🔍 搜索添加  │  ⚙ 管理关注   │      │
│  └────────────────────────────────┘      │
└─────────────────────────────────────────┘
```

### 页面清单

| 页面 | 入口 | 关键组件 | iPhone 适配 | iPad 适配 |
|------|------|----------|-------------|-----------|
| **关注列表（首页）** | 启动默认 | 平台 Tab + 直播/视频卡片 | 单列 | 双列或三列 |
| **直播间** | 点击直播入口 | IJKPlayer + 弹幕叠加 + 房间信息 | 竖屏全屏 | 横屏沉浸，弹幕区域更大 |
| **视频播放** | 点击视频卡片 | IJKPlayer + 弹幕 + 评论抽屉 | 竖屏 | 支持横竖切换 |
| **UP 主主页** | 点击头像/昵称 | 头像信息 + 视频瀑布流 | 单列 | 双列 |
| **添加关注** | 搜索按钮 | 输入框（UID/链接）+ 搜索结果 | Sheet | SidePanel 或 Sheet |
| **管理关注** | 设置按钮 | 关注列表 + 左滑删除 | List | 表格布局 |

### iPad 特殊适配

- 直播间默认横屏，弹幕区域可调大小
- 关注列表使用 `NavigationSplitView` 实现侧边栏 + 详情的经典 iPad 布局
- 视频播放支持画中画（PiP）

---

## 四、各场景实现路径

### S1：通勤刷更新（iPhone）

```
App 启动 → 并行请求抖音/B站关注列表的最新数据
         → 渲染关注列表（直播置顶、视频按时间倒序）
         → 用户点击视频 → VideoPlayerView 播放
```

- 数据拉取：遍历本地 FollowedCreator，并行调用各平台 API 获取最新视频和直播状态
- 冷启动优化：先展示本地缓存的上次数据，后台刷新后更新

### S2：睡前看直播（iPad）

```
点击直播间 → 调 API 获取拉流地址 → IJKPlayer 播放 FLV
          → 同时建立 WebSocket 连接 → 实时弹幕
          → DanmakuCanvas 叠加在视频上方
```

- FLV 流延迟 ~3s，可接受
- 弹幕 WebSocket 断线自动重连（指数退避）

### S3：补历史视频

```
点击 UP 主头像 → 调 API 获取用户视频列表（分页）
              → 瀑布流展示 → 点击视频 → 播放 + 弹幕 + 评论
```

- 历史弹幕：B 站有历史弹幕 API（按时间片段请求），抖音历史弹幕待确认
- 评论：视频下方抽屉式展开

### S4：找新主播

```
输入 UID → 调 API 获取用户信息 → 确认 → 保存到本地
```

- 支持粘贴主页链接，正则提取 UID
- B 站也可输入 mid

### S5：管理关注

```
关注列表 → 左滑删除 → 确认弹窗 → 移除本地记录
```

---

## 五、风险矩阵与缓解策略

| 风险 | 概率 | 影响 | 缓解策略 |
|------|------|------|----------|
| 抖音签名算法变更 | 中高 | 高 | WKWebView + JS 方案自动跟随更新；定期关注 MediaCrawler / Simple Live 等项目的更新 |
| IJKPlayer 停更导致编译失败 | 低 | 中 | FFmpeg 本身稳定，如有问题可切到自编译 FFmpeg 或 AVPlayer(HLS) |
| 抖音/B 站限制非官方客户端清晰度 | 中 | 低 | 需求已明确接受 720p 限制 |
| 抖音弹幕签名失效 | 中 | 中 | 同签名变更缓解策略；弹幕为增强功能，失效时可降级为无弹幕直播 |
| B 站弹幕协议变更 | 低 | 低 | 协议多年稳定，且社区实时跟进 |
| App 审核风险 | 无 | 无 | 不上架，仅自用 Xcode 安装 |

---

## 六、建议的开发优先级

```
Phase 1（核心可用）≈ 2-3 周
  ├─ B 站：关注管理 + 用户视频列表 + 视频播放 + 历史弹幕 + 评论
  ├─ 通用：本地持久化 + 基础 UI 框架
  └─ 原因：B 站接口最稳定，先做能跑通的最小闭环

Phase 2（直播能力）≈ 1-2 周
  ├─ B 站：直播间 + 实时弹幕 WebSocket
  ├─ IJKPlayer 集成
  └─ 弹幕渲染组件

Phase 3（抖音接入）≈ 2-3 周
  ├─ 抖音：签名方案（WKWebView）+ API 对接
  ├─ 抖音：视频列表 + 视频播放
  └─ 抖音：直播间 + 实时弹幕

Phase 4（打磨）≈ 1 周
  ├─ iPad 大屏适配
  ├─ 横屏沉浸模式
  ├─ 画中画
  └─ 断网空状态、错误处理
```

---

## 七、关键决策点（需要你确认）

1. **最低 iOS 版本**：iOS 16 还是 iOS 17？影响是否可用 SwiftData。
2. **签名方案**：WKWebView + JS（推荐，低维护）还是纯 Swift 逆向（高性能但高维护）？
3. **播放器**：IJKPlayer（推荐，全格式支持）还是 AVPlayer + 跳转抖音（简单但体验差）？
4. **抖音弹幕**：v0.1 是否必须？可否先不做抖音弹幕，只做 B 站弹幕？
5. **TikHub.io 等付费 API**：是否考虑作为备选或降级方案？

---

## 附录：关键参考项目

| 项目 | 语言 | 功能 | 对本项目的价值 |
|------|------|------|----------------|
| [dart_simple_live](https://github.com/xiaoyaocz/dart_simple_live) | Dart | 抖音/B 站直播 + 弹幕 + 关注 | **核心参考**：实现逻辑可直接移植 |
| [MediaCrawler](https://github.com/NanmiCoder/MediaCrawler) | Python | 抖音/B 站数据爬取 | 签名方案参考（Playwright + JS） |
| [bilibili-API-collect](https://github.com/SocialSisterYi/bilibili-API-collect) | Docs | B 站全量 API 文档 | B 站接口权威参考 |
| [Bilibili-Live-API](https://github.com/lovelyyoshino/Bilibili-Live-API) | Docs | B 站直播 WebSocket 文档 | 弹幕协议实现参考 |
| [bilibili-live-danmaku](https://github.com/Minteea/bilibili-live-danmaku) | TS | B 站弹幕 WebSocket 实现 | 含 proto 文件，可直接翻译为 Swift |
| [dycast](https://github.com/skmcj/dycast) | TS | 抖音弹幕 WebSocket 实现 | 含 proto 文件和签名逻辑 |
| [bilibili/ijkplayer](https://github.com/bilibili/ijkplayer) | C/OC | iOS FLV/HLS/RTMP 播放器 | 直播/视频播放核心组件 |
| [Douyin_TikTok_Download_API](https://github.com/Evil0ctal/Douyin_TikTok_Download_API) | Python | 抖音网页端 API 集合 | 接口路径和参数参考 |
